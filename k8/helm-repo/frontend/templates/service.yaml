apiVersion: v1
kind: Service
metadata:
  name: frontendapp
  labels:
    app: {{ .Values.labels.app }}
spec:
  type: NodePort
  selector:
      app: {{ .Values.labels.app }}
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
      nodeport: 32146
 
